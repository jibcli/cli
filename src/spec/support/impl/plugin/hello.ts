/**
 * Define contract for plugin registrations
 * @todo
 * 
 * ```
 * {
 *   decorators: [{}], // apply decorator functions to core classes?
 *   extensions: [], // apply extensions to core classes?
 * }
 * 
 * ```
 * - order of plugins matters
 * - use @PluginDeco<type>(...) to decorate?
 * - apply decorators inside the @Command decorator?
 */
export const PluginConfig = {
  
};